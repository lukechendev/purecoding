/**
 * 1. behavior question, tell an inappropriate technology case.
 * Talked about the design of the static map files loaded from theme data packages vs. from a non-public theme config location.
 *
 * 2. Design and implement an cache manager. The manager can install different cache policies, LRU, LFU, max entries, max memory usage, etc. based on configurations.
 * Implement the LRU using the cache manager.
 **/
